Index: asciiart/CMakeLists.txt
===================================================================
--- asciiart.orig/CMakeLists.txt
+++ asciiart/CMakeLists.txt
@@ -11,7 +11,6 @@ if(NOT CMAKE_BUILD_TYPE)
     set(CMAKE_BUILD_TYPE "Release")
 endif()
 
-find_package(cxxopts REQUIRED)
 find_package(PkgConfig REQUIRED)
 find_package(libavif QUIET)
 if(libavif_FOUND)
